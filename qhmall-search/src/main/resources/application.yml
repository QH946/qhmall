spring:
  thymeleaf:
    cache: false
  redis:
    port: 6379
    host: 192.168.174.130
  session:
    store-type: redis

cloud:
  sentinel:
    transport:
      dashboard: localhost:8333

management:
  endpoints:
    web:
      exposure:
        include: "*"
feign:
  sentinel:
    enabled: true

logging:
  level:
    com.qh.qhmall.search.service: debug
